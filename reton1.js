let frase = prompt('Ingrese una Cadena de Texto', '')
let X = 0
let letra = frase.charAt(X)
let contador = 0
let longitud = frase.length


if (letra === a, e, i, o, u) {
    contador = contador + 1;
    X = X + 1;
}
else {( X = X + 1)
}

alert("Tienes " + contador + " Vocales");

